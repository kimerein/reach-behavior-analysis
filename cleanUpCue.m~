function cleanUpCue(aligned)

% This is the fraction of range above the min to use as cut-off threshold
% for cue on
thresh=0.5; 

temp=nan(size(aligned.cueZone));
isOn=aligned.cueZone>thresh;
temp(isOn)=1;
temp(~isOn)=0;
aliged.